<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page.title %> | <%= siteName %></title>
    <% if (page.meta_title) { %><meta name="title" content="<%= page.meta_title %>"><% } %>
    <% if (page.meta_description) { %><meta name="description" content="<%= page.meta_description %>"><% } %>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <%- include('partials/public-header', { currentPage: 'page-' + page.slug, siteName: siteName, pages: pages }) %>

    <!-- Breadcrumb -->
    <div class="bg-gray-100 py-4">
        <div class="max-w-4xl mx-auto px-4">
            <nav class="text-sm">
                <a href="/" class="text-blue-600 hover:text-blue-800">Ana Sayfa</a>
                <span class="mx-2 text-gray-500">/</span>
                <span class="text-gray-600"><%= page.title %></span>
            </nav>
        </div>
    </div>

    <!-- Page Content -->
    <div class="max-w-4xl mx-auto px-4 py-12">
        <div class="bg-white rounded-lg shadow-lg overflow-hidden">
            <!-- Page Header -->
            <div class="p-8 border-b border-gray-200">
                <h1 class="text-4xl font-bold text-gray-800 mb-4"><%= page.title %></h1>
                <div class="flex items-center text-sm text-gray-500">
                    <i class="fas fa-calendar mr-2"></i>
                    <span>Oluşturuldu: <%= new Date(page.created_at).toLocaleDateString('tr-TR') %></span>
                    <% if (page.updated_at && page.updated_at !== page.created_at) { %>
                        <span class="mx-2">•</span>
                        <i class="fas fa-edit mr-2"></i>
                        <span>Güncellendi: <%= new Date(page.updated_at).toLocaleDateString('tr-TR') %></span>
                    <% } %>
                </div>
            </div>

            <!-- Page Content -->
            <div class="p-8">
                <div class="prose prose-lg max-w-none">
                    <%- page.content.replace(/\n/g, '<br>') %>
                </div>
            </div>

            <!-- Page Footer -->
            <div class="p-8 border-t border-gray-200 bg-gray-50">
                <div class="flex items-center justify-between">
                    <div>
                        <h3 class="text-lg font-semibold text-gray-800 mb-2">Daha fazla bilgi mi arıyorsunuz?</h3>
                        <p class="text-gray-600">Diğer sayfalarımıza göz atın veya blog yazılarımızı okuyun.</p>
                    </div>
                    <div class="flex space-x-4">
                        <a href="/" class="bg-gray-600 text-white px-4 py-2 rounded-lg font-semibold hover:bg-gray-700 transition-colors">
                            <i class="fas fa-home mr-2"></i>
                            Ana Sayfa
                        </a>
                        <a href="/blog" class="bg-blue-600 text-white px-4 py-2 rounded-lg font-semibold hover:bg-blue-700 transition-colors">
                            <i class="fas fa-blog mr-2"></i>
                            Blog
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-12">
        <div class="max-w-6xl mx-auto px-4">
            <div class="text-center">
                <h3 class="text-xl font-bold mb-4"><%= siteName %></h3>
                <p class="text-gray-400 mb-6">Sayfalarımızda size en iyi deneyimi sunmaya çalışıyoruz.</p>
                <div class="flex justify-center space-x-6">
                    <a href="/" class="text-blue-400 hover:text-blue-300 transition-colors">Ana Sayfa</a>
                    <a href="/blog" class="text-blue-400 hover:text-blue-300 transition-colors">Blog</a>
                </div>
            </div>
            <div class="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400">
                <p>&copy; <%= new Date().getFullYear() %> <%= siteName %>. Tüm hakları saklıdır.</p>
            </div>
        </div>
    </footer>
</body>
</html>